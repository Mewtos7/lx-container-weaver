# golangci-lint configuration for LX Container Weaver.
# Docs: https://golangci-lint.run/usage/configuration/

version: "2"

linters:
  default: none
  enable:
    - errcheck
    - govet
    - staticcheck

run:
  timeout: 5m
